#include <bits/stdc++.h>
using namespace std;

#define ll long long
int digit(ll k)
{
    
}
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    int q;
    cin >> q;

    while(q--)
    {
        ll n;
        cin>>n;
        cout<digit(n)<<"\n";
    }
    return 0;
}